# React 19 is newer than some peer-dependency ranges declared by libraries
# in this project (parts of the three.js/R3F ecosystem in particular) —
# this stops npm's strict peer-dependency resolution from failing the
# install over version ranges that work fine in practice.
legacy-peer-deps=true
